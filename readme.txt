hello world
git init initialize local repo
git add <file> add files to staging area
git status check status of files in staging area
git commit -m "message" commit changes  to local repo
git push push local repo to remote repo
git pull pull hanges from remote repo
git clone copy a remote repo into new dr
git rm -cached <file> removes file from repo
git config -global user.name "name"
git config -global user.email "email"
git branch <name> make a new branch
git checkout <branch name> change branch
git merge <branch> merges branch to master
git remove add <name> <url> adds a new repo
git push -u <name> <branch> pushes changes to remote repo
touch <name> adds to repository